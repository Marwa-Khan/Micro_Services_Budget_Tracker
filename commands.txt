
pip install "pydantic<2.0"


#psycopg2 2.9.10(first need to have microsoft c++ toolkit )
pip install psycopg2

#to see installed version of psycopg2
pip show psycopg2


#paho-mqtt 2.1.0
pip install paho-mqtt

pip install uvicorn


# Create a virtual environment
python -m venv venv

# Activate the virtual environment
# On Windows:
venv\Scripts\activate
#on mac
# For zsh (or bash on macOS)
source venv/bin/activate


#install packages from requirements.txt
pip install -r requirements.txt




#run each service seperately 
uvicorn main:app --reload --host 127.0.0.1 --port <PORT>
#Replace <PORT> with the desired port for each service (e.g., 8001, 8002, etc.)


# uvicorn auth_service.main:auth_service --reload --host 0.0.0.0 --port 8001
#running commands because we are running services as independant of others
uvicorn main:auth_service --reload --host 127.0.0.1 --port 8001


uvicorn main:expense_service --reload --host 127.0.0.1 --port 8002


uvicorn main:account_service --reload --host 127.0.0.1 --port 8003



docker run -d --name rabbitmq -p 5672:5672 -p 15672:15672 rabbitmq:3-management


http://localhost:15672


docker run --rm -it --name expense_service_newcheck -e MQTT_URL=mqtt.bibimarw.2.rahtiapp.fi -e MQTT_PORT=1883 -e CLIENT_ID=bibimarw marwakhan/expense_service:1.0



SENDGRID_API_KEY = "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"

